import 'package:flutter/material.dart';

class IndividualUserData extends StatelessWidget {
  const IndividualUserData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
